#include "algo_hw2.hpp"
#include <bits/stdc++.h>
using namespace std;
int main()
{
    std::vector<int> test_array(46,-1);
    cout<<"Test array after insertion result: ";
    for(int i=0;<test_array.size();i++)
    {
        cout<<test_array[i]<<" ";
    }

    while(1)
    {
        cout<<"DeleteRBT test insert a key to delete, and insertt -1 to terminate ";

    }
    while(1)
    {
        cout<<"Query the Order Statistics, find the ith smallest element ,enter i";
        
    }




}
